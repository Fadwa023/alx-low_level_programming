#include "main.h"
/**
 * mul -  multiplies two integers
 *
 * @a: The first integer
 * @b: The second integer
 *
 * Return: Always result of the multiply of two integer
 */

int mul(int a, int b);
{
return (a * b);
}
